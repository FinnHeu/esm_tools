general:
    user: !ENV ${USER}
    setup_name: "awicm3"
    version: "v3.3.0"
    account: "ba1550"
    compute_time: "04:00:00"
    initial_date: "1850-01-01"
    final_date: "1860-01-01"
    base_dir: "/work/ab0995/${user}/esm_tools/runtime/${general.setup_name}-${general.version}/preproduction/"
    pool_dir: "/work/ab0995/${user}/model_inputs/awicm3/pool/"
    nday: 0
    nmonth: 0
    nyear: 10
    use_venv: false
    
computer:
    taskset: true

awicm3:
    postprocessing: false
    model_dir: "/work/ab0995/${user}/esm_tools/model_codes/${general.setup_name}-${general.version}/baseline/${general.setup_name}-${general.version}/"

fesom:
    resolution: "CORE2ice"
    mesh_dir: "/work/ab0995/a270186/model_inputs/fesom2/mesh/CORE2ice/"
    restart_rate: 10
    restart_unit: "y"
    restart_first: 10
    time_step: 1200 # was 450 for restart which worked
    nproc: 768
    omp_num_threads: 4
    tar_binary_restarts: true
    lresume: true
    ini_parent_exp_id: "SPIN"
    ini_parent_dir: "/work/bb1469/a270092/runtime/awicm3-v3.3.0/${ini_parent_exp_id}/"
    ini_parent_date: "${prev_date}"
    choose_general.run_number:
        1:
            lasttime: 84600
            restart_in_sources:
                par_oce_restart: "${general.base_dir}/restarts_CORE2_to_CORE2ice/SPIN/fesom.1849.oce.restart/*.nc"
                par_ice_restart: "${general.base_dir}/restarts_CORE2_to_CORE2ice/SPIN/fesom.1849.ice.restart/*.nc"
    add_namelist_changes:
        namelist.config:
            run_config:
                use_cavity: true

oifs:
    resolution: "TCO95"
    levels: "L91"
    prepifs_expid: aack
    input_expid: awi3
    wam: true
    lresume: false
    time_step: 3600
    nproc: 384
    omp_num_threads: 4
    add_namelist_changes:
        fort.4:
            NAERAD:
                LCMIP6: true
                NCMIPFIXYR: 1850
                SSPNAME: 'historical'

oasis3mct:
    lresume: true # Set to false to generate the rst files for first leg
    time_step: 3600

xios:
    with_model: oifs
    nproc: 8
    omp_num_threads: 32

